## Naming convention
Naming convention will always be with the following format:
<resource-type>-<workload>-<environment>-<region>-<instance>

Always use the set naming convention when working on resource creation.

## Taggin
Mandatory tags:
environment, workload, costCenter, owner, deployedBy (bicep)


